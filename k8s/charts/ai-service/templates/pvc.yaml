apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "ai-service.fullname" . }}-pvc
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - {{ .Values.persistence.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
